<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <queries>
        <package android:name="com.snapchat.android" />
    </queries>

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:supportsRtl="true">

        <!-- Snap Kit OAuth2 Client ID -->
        <meta-data
            android:name="com.snapchat.kit.sdk.clientId"
            android:value="7ed38603-427a-4042-aefb-c7acd1f4a7d3" />

        <!-- Snap Kit Redirect URL -->
        <meta-data
            android:name="com.snapchat.kit.sdk.redirectUrl"
            android:value="solaridx://" />

        <!-- Snap Kit Scopes -->
      <meta-data
    android:name="com.snapchat.kit.sdk.scopes"
    android:resource="@array/SCSDKScopes" />


        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Snap Kit Activity -->
        <activity
            android:name="com.snapchat.kit.sdk.SnapKitActivity"
            android:launchMode="singleTask"
            android:exported="true"> <!-- Explicitly set android:exported -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Updated with parts of your redirect URI -->
                <data
                    android:scheme="solaridx"
                    />
            </intent-filter>
        </activity>

    </application>
</manifest>
